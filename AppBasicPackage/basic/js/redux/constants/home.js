/**
 * @author Yasin
 * @version [React-Native V01, 2017/10/20]
 * @date 2017/10/20
 * @description home.js
 */
const NAME = 'HOME';

const INIT_APP = `${NAME}/INIT_APP`;
const RECEIVER_TRIP = `${NAME}/RECEIVER_TRIP`;
const ACCEPT_TRIP = `${NAME}/ACCEPT_TRIP`;

export default homeTypes = {
    NAME,
    INIT_APP,
    RECEIVER_TRIP,
    ACCEPT_TRIP,
};
